# Alias de tu cuenta en el keystore de Stellar CLI.
# Debe tener XLM (para fees) y al menos 10 USDC en testnet.
#
# Setup:
#   stellar keys generate --name <tu-alias> --network testnet
#
# Luego copia este archivo a .env y rellena tu alias:
#   cp .env.example .env

DEPLOYER=tu-alias-aqui
